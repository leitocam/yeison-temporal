# Production Environment Variables
# Use relative path to proxy through Netlify (avoids CORS and mixed content)
NEXT_PUBLIC_API_URL=/api/v1
NEXT_PUBLIC_APP_NAME=Yeison
NEXT_PUBLIC_ENABLE_MOCK_AUTH=false
